{
  "name": "aleph-view-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check --write . && bun run typecheck && bun test",
    "build:site": "ALEPH_HOST=../www/host.jsonld bun run --cwd packages/shell build && bun run --cwd packages/www build && cp -R packages/shell/dist/. packages/www/dist/"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@happy-dom/global-registrator": "^20.14.5",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
