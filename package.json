{
  "name": "vitrine-workspace",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:packages": "bun scripts/build-package.ts",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check --write . && bun run typecheck && bun test",
    "check:ci": "biome check . && bun run typecheck && bun test",
    "build:site": "ALEPH_NOTE=../../docs/vision.md bun run --cwd packages/garden-host build && bun run --cwd packages/www build && cp -R packages/garden-host/dist/. packages/www/dist/",
    "build:pod": "bun run --cwd packages/pod-host build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@happy-dom/global-registrator": "^20.14.5",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
